<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.EPoliceManagementSystem" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="buttonStyle">@drawable/button</item>
        <item name="pageBackground">@drawable/page_background</item>
        <item name="textSub_header">@drawable/text_subheader</item>
        <item name="textLarge">@drawable/text_large</item>
        <item name="textRegular">@drawable/button_default</item>
        <item name="whiteBackground">@drawable/white_background</item>
        <item name="button">@drawable/button</item>

    </style>

    <style name="Theme.EPoliceManagementSystem.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.EPoliceManagementSystem.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.EPoliceManagementSystem.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>